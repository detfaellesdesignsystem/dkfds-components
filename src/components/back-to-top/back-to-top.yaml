params:
- name: topLink
  type: string
  required: true
  description: The link to activate when the button is pressed.
- name: buttonText
  type: string
  required: false
  description: The text written on the back-to-top button. Defaults to 'Til toppen'.
