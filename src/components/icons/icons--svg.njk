{% macro icon_svg(iconname, classes, arialabel, ariahidden, focusable, tabindex) %}<svg class="icon-svg{% if classes %} {{ classes }}{% endif %}"{% if arialabel %} aria-label="{{arialabel}}"{% endif %} focusable="{{focusable|default('false') }}" aria-hidden="{{ariahidden|default('true') }}"{% if tabindex %} tabindex="-1"{% endif %}><use xlink:href="#{{iconname}}"></use></svg>{% endmacro %}
